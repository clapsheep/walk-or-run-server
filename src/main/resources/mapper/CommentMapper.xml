<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wor.dash.challenge.model.mapper.CommentMapper">
    <insert id="insertComment" parameterType="comment">
        INSERT INTO Comments
            (challenge_id, comment_content, comment_author_id, comment_create_date)
        VALUES (#{challengeId}, #{commentContent}, #{commentAuthorId}, now())
    </insert>

    <select id="selectCommentList" parameterType="int" resultType="comment">
        SELECT comment_id,
               challenge_id,
               comment_content,
               comment_author_id,
               comment_create_date
        FROM Comments
        WHERE challenge_id = #{challengeId}
        ORDER BY comment_create_date DESC
    </select>

    <update id="updateComment" parameterType="comment">
        UPDATE Comments
        SET comment_content = #{commentContent}
        WHERE comment_id = #{commentId}
    </update>
    
    <delete id=" deleteComment" parameterType="int">
        DELETE
        FROM Comments
        WHERE Comment_id = #{commentId}
    </delete>


</mapper>